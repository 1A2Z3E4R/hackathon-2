{% extends 'base.html.twig' %}

{% block body %}
    <div class="admin">
    <div class="row">
        <h1 class="my-3 mx-auto">Game edit</h1>
    </div>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input class="btn btn-outline-secondary my-3 mx-1" type="submit" value="Edit"/>
    {{ form_end(edit_form) }}

        <a class="btn btn-outline-secondary my-3 mx-1" href="{{ path('admin_game_index') }}">Back to the list</a>
        {{ form_start(delete_form) }}
        <input class="btn btn-outline-secondary my-3 mx-1" type="submit" value="Delete">
        {{ form_end(delete_form) }}
    </div>
{% endblock %}
